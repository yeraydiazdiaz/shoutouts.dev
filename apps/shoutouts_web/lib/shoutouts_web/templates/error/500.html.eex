<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>shoutouts.dev</title>
    <link rel="stylesheet" href="/css/app.css"/>
    <meta name="description" content="Open Source is hard. Show your gratitude.">
    <meta name="author" content="ShoutOuts.dev">
    <meta name="keywords" content="opensource open source projects developer shout-out shoutout shoutouts">
  </head>
  <body class="flex flex-col justify-between h-screen bg-dimmer">
    <header class="flex-none flex flex-row items-center bg-primary text-dim shadow-bottom h-header px-4 lg:px-8 py-2">
      <div class="mx-auto flex flex-row justify-between items-center w-full max-w-screen-xl">
        <a class="flex flex-row items-center font-logo text-2xl lg:text-3xl" href="/">
          <div class="flex flex-col">
            <span class="tracking-tight">shoutouts.dev</span>
            <span class="text-sm text-alert text-right p-0 -mt-4 mr-2">BETA</span>
          </div>
          <img src="<%= Routes.static_path(@conn, "/images/logo.svg") %>" alt="shoutouts.dev logo" width="50px" height="50px" />
        </a>
        <nav role="navigation">
          <div class="hidden lg:flex lg:flex-row lg:items-center">
            <div class="inline-block py-1 px-4 mr-8 bg-dim text-gray-400 rounded-md outline-none focus:shadow-inner focus:border focus:border-dim" />
              <i class="fas fa-search"></i>
              <form action="/search" method="GET" class="inline">
                <input type="text" name="q" placeholder="Search projects" class="ml-2 text-dark appearance-none outline-none bg-transparent" />
              </form>
            </div>
              <a class="inline-block font-bold hover:underline text-center" href="<%= Routes.auth_path(@conn, :request, :github) %>">
                <div class="flex flex-row items-center"><i class="fab fa-github fa-2x"></i><span class="ml-2">Login with GitHub</span></div>
              </a>
          </div>
          <div id="nav-bars" class="lg:hidden text-lg cursor-pointer"><i class="fas fa-bars"></i></div>
          <div id="nav-menu" class="hidden absolute top-0 right-0 mt-16 flex flex-col bg-white text-secondary border border-secondary">
            <a class="inline-block text-primary font-bold hover:underline text-center px-6 py-4" href="<%= Routes.search_show_path(@conn, :index) %>">Search</a>
              <a class="inline-block text-primary font-bold hover:underline text-center px-6 py-4" href="<%= Routes.auth_path(@conn, :request, :github) %>">
                <div class="flex flex-row items-center"><i class="fab fa-github fa-2x"></i><span class="ml-2">Login with GitHub</span></div>
              </a>
          </div>
        </nav>
      </div>
    </header>
    <main role="main" class="flex-grow m-auto p-4 md:p-8 w-full max-w-6xl">
      <div class="bg transform translate-y-8 md:-translate-y-16"></div>
      <%# --- CONTENT STARTS HERE --- %>
      <section class="flex flex-col lg:flex-row lg:justify-around lg:items-center m-auto w-full mt-8 mb-12">
        <h1 class="tracking-tight text-4xl sm:text-5xl md:text-6xl text-center lg:text-left text-dim font-bold max-w-screen-xlm-auto">
          <span style="text-shadow: 0 0.1rem 0.2rem rgba(0, 0, 0, 0.22)">Oops, something's wrong</span>
        </h1>
      </section>
      <div>
        <p class="text-xl text-center mb-4">That didn't quite compute, sorry about that, we got an alert though so we'll have a look at the problem soon.</p>
        <p class="text-xl text-center">You may want <a href="/" class="link">start over</a>, or <a href="<%= Routes.search_show_path(@conn, :index) %>" class="link">search for registered projects</a>.</p>
      </div>
      <%# --- CONTENT STOPs HERE --- %>
    </main>
</html>
